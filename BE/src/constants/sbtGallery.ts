/** Giới hạn phân trang công khai của SBT gallery, dùng chung giữa service và validator. */
export const SBT_GALLERY_MAX_PAGE = 500;

/** Số SBT tối đa mỗi response gallery để giới hạn chi phí query và batch lookup project. */
export const SBT_GALLERY_PAGE_SIZE = 20;
