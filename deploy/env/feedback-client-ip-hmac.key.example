# Không dùng file mẫu này làm runtime secret.
# Tạo /opt/dcp/env/feedback-client-ip-hmac.key với một chuỗi ngẫu nhiên >=32 ký tự,
# chmod 600, rồi Compose sẽ mount read-only cho backend, worker và frontend.
