/** So tien nap toi thieu cho guest deposit (token). Gioi han nay dam bao VNĐ tương ứng đủ lon de donate. */
export const MIN_GUEST_DEPOSIT_AMOUNT = 10000;

/** So tien nap toi da cho guest deposit (token). Gioi han nay ngan chan abuse. */
export const MAX_GUEST_DEPOSIT_AMOUNT = 200000;
