import AuditorPortalClient from '../components/governance/AuditorPortalClient';
import type { ReactElement } from 'react';

/** Hiển thị cổng nghiệp vụ dành riêng cho Kiểm toán viên. */
export default function AuditorPage(): ReactElement {
  return <AuditorPortalClient />;
}
